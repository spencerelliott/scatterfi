<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.spencerelliott.scatterfy"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:targetSdkVersion="15" android:minSdkVersion="11"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ServerActivity"></activity>
        <service android:name="ca.spencerelliott.scatterfy.services.ServerService"></service>
        <service android:name="ca.spencerelliott.scatterfy.services.ClientService"></service>
        <activity android:name=".ClientActivity"></activity>
    </application>

</manifest>